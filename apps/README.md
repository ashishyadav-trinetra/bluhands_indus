# apps — PLANNED (T-A03 / T-A05) · user-facing frontends

- `onboarding/` — Next.js onboarding + merchant dashboard (talks to control-plane `/api/v1`).
- `starters/`   — golden frontend starters the agent edits (shadcn + design tokens).
