# Storefront → Medusa connection. The agent fills these per tenant from the
# capability manifest + control plane. Copy to .env.local for local dev.
NEXT_PUBLIC_MEDUSA_URL=http://localhost:9000
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=
